@model TaskView

<form id="{@Model.Id}_EditForm">
    <input id="{@Model.Id}_EditInput" type="text" class="form-control">
    <button id="{@Model.Id}_SaveChanges" type="submit" class="btn btn-light">
        <i class="bi bi-save"></i>
    </button>
</form>